httpx>=0.27
orjson>=3.9
python-dotenv>=1.0
# tinydb is not used at runtime any more; it is kept so that the on-disk format
# of kifu_db.json can be verified against the real reader (tests/test_storage.py).
tinydb>=4.8
